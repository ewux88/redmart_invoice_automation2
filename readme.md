
# Extract pdfs from email
    - search for emails from past month
    - select emails with msg "your redmart invoice" AND has:attachment 
    - output new pdfs that are not already in google drive

# For each pdf:
    # Extract fields from invoice pdf
    # Append data to google sheets
    # Upload to google drive
